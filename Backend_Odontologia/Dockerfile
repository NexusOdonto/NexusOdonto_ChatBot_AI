FROM alpine:3.19

# Lightweight placeholder Dockerfile for .NET Backend API until C# codebase is mounted
RUN apk add --no-cache python3

WORKDIR /app

EXPOSE 5000

CMD ["python3", "-m", "http.server", "5000"]
